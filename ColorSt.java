/**
 * A ColorSt enum represents the colors and the pieces on the board.
 * It only has two values, but the null-value of this enum is used to represent a empty piece. *
 */
public enum ColorSt
{
    WHITE(),
    BLACK()
}


